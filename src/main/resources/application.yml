spring:
  application:
    name: devops

  datasource:
    url: ${AWS_RDS_URL}
    username: ${RDS_USERNAME}
    password: ${RDS_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8080

aws:
  accessKey: ${AWS_ACCESS_KEY_ID:000}
  secretKey: ${AWS_SECRET_ACCESS_KEY:000}
  region: ${AWS_REGION:AP_SOUTH_1}
  s3:
    bucket: files-bucket-kb


